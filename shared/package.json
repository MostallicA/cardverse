{
  "name": "@cardverse/shared",
  "version": "0.1.0",
  "description": "CardVerse shared utilities and types",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.3.3"
  }
}
